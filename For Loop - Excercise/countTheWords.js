function countTheWords (input) {
    let textLength = input[0];
   console.log(textLength);
    // for(let i = 0; i =a  ; i++) {

    // }
}
countTheWords(["This message has exactly eleven words. One more as it's allowed!"])